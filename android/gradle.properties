org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# Do not set org.gradle.java.home here — it is machine-specific and breaks
# every other environment (CI included). Put it in ~/.gradle/gradle.properties
# instead, or export JAVA_HOME.
